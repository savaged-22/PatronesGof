spring:
  application:
    name: music-shop
  datasource:
    url: "jdbc:postgresql://4.tcp.ngrok.io:16786/transmilenio_db"
    username: savaged22
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: never
      