spring:
  application:
    name: music-shop
  datasource:
    url: "jdbc:postgresql://4.tcp.ngrok.io:12394/transmilenio_db"
    username: savaged22
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
        boot:
          allow_jdbc_metadata_access: false
  sql:
    init:
      mode: never